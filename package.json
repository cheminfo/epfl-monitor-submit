{
  "name": "epfl-monitor-submit",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "./backend",
    "./frontend"
  ],
  "scripts": {
    "eslint": "eslint --cache .",
    "eslint-fix": "node --run eslint -- --fix",
    "prettier": "prettier --check .",
    "prettier-write": "prettier --write .",
    "test": "npm run eslint && npm run prettier && npm run test-only",
    "test-only": "NODE_OPTIONS=\"--experimental-webstorage --localstorage-file=:memory:\" vitest --run"
  },
  "devDependencies": {
    "@preact/signals-react-transform": "^0.5.1",
    "eslint": "^9.17.0",
    "eslint-config-zakodium": "^14.0.0",
    "prettier": "^3.4.2",
    "vitest": "^3.0.5"
  },
  "volta": {
    "node": "22.13.1"
  }
}
